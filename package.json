{
  "name": "portfolio-desh",
  "version": "0.1.0",
  "license": "ISC",
  "type": "commonjs",
  "scripts": {
    "build": "npx mz b",
    "start": "npx mz b && npx serve dist/",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky || true"
  },
  "dependencies": {
    "bootstrap-icons": "^1.13.1",
    "minimaz-cli": "^0.5.2"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.9.6",
    "serve": "^14.2.6"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  }
}
